<h1> This is a Pretty fast authors index page. One of the fastest ever. </h1>

<nav style="background-color: #9AEEE8">
  <%= link_to "Stats", stats_index_path %>
</nav>

<div class="author">
  <% @authors.each do |author| %>

      <h2>My name is <%= author.name %></h2>

        <% author.articles.each do |art| %>
          <p>I wrote: <span style="text-decoration: underline"><%= link_to art.name, "articles/show/#{art.id}" %> </span></p>
        <% end %>
  <% end %>
  <%= will_paginate @authors %>
</div>
